# UFPE
# Segundo Período
```dataview
list 
where 
  contains(tags, "HUB") 
  and contains(tags, "UFPE") 
  and PERIODO = 2
sort file.name asc
```
# Terceiro Período
```dataview
list 
where 
  contains(tags, "HUB") 
  and contains(tags, "UFPE") 
  and PERIODO = 3
sort file.name asc
```
# Quarto Período
```dataview
list 
where 
  contains(tags, "HUB") 
  and contains(tags, "UFPE") 
  and PERIODO = 4
sort file.name asc
```